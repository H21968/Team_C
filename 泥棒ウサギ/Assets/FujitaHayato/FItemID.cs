using UnityEngine;

public class FItemID : MonoBehaviour
{
    public int itemId;      // アイテムを識別するID
    public string itemName; // 名前（デバッグ用）

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
